# Deployment

- Production branch: `main` (the only branch as of 2026-09-25)
- Canonical entrypoint: `index.html` / `day.html` — the single-workday core-loop build (#74)
- Archived reference build: `week.html` — the original one-week prototype, not under active development
- Static browser app, no required environment variables
- Live AI is not enabled in this build.

This file also provides a clean Git-trigger point for the first Vercel preview after project import.
